- [x] Use schemathesis for testing
- [x] Fix /authorize endpoint to comply with OIDC spec. It needs to respond with redirect, and pass `code` parameter. For now it will be static with no validation.
- [] Write correct OpenAPI docs for /authorize, since the autodocs cant write about redirect.

